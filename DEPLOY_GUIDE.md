# 🚀 Netlify 배포 가이드 (초보자용)

## 📌 핵심 정리

**`npm run dev`는 로컬 테스트용입니다!**
- 로컬에서만 확인 (http://localhost:3000)
- Netlify 배포와는 **관계 없음**

**Netlify 배포는 Git 푸시만 하면 자동으로 됩니다!**

---

## ✅ 가장 쉬운 배포 방법 (3단계)

### 1단계: 코드 수정 후 저장
- Cursor에서 파일 수정
- `Ctrl + S`로 저장

### 2단계: Git에 커밋 및 푸시
터미널에서 다음 명령어 3개만 입력:

```bash
git add .
git commit -m "수정 내용 설명"
git push origin main
```

### 3단계: Netlify 자동 배포 대기
- Netlify가 자동으로 감지하고 배포 시작
- 2-5분 후 완료
- Netlify 대시보드에서 확인

---

## 📝 상세 설명

### 로컬 테스트 (선택사항)

**언제 사용하나요?**
- 배포 전에 로컬에서 먼저 확인하고 싶을 때
- Netlify 배포와는 **별개**입니다

**방법:**
```bash
npm run dev
```
- 브라우저에서 `http://localhost:3000` 열기
- 수정사항 확인
- **중요**: 이건 로컬에서만 보이는 거예요!

---

### 실제 배포 (Netlify)

**자동 배포 설정이 되어 있다면:**
1. GitHub에 푸시 (`git push`)
2. Netlify가 자동으로 감지
3. 자동으로 빌드 및 배포
4. 완료!

**수동으로 재배포하려면:**
1. Netlify 대시보드 접속
2. 사이트 선택
3. "Deploys" 탭 클릭
4. "Trigger deploy" → "Deploy site" 클릭

---

## 🔄 전체 워크플로우

```
1. Cursor에서 코드 수정
   ↓
2. 파일 저장 (Ctrl + S)
   ↓
3. 터미널에서:
   git add .
   git commit -m "수정 내용"
   git push origin main
   ↓
4. GitHub에 코드 업로드됨
   ↓
5. Netlify가 자동 감지
   ↓
6. Netlify가 자동 빌드 및 배포
   ↓
7. 2-5분 후 배포 완료!
   ↓
8. Netlify에서 제공한 URL로 접속하여 확인
```

---

## 💡 자주 묻는 질문

### Q: `npm run dev`를 실행해야 하나요?
**A:** 아니요! 로컬에서만 테스트하고 싶을 때만 사용하세요. Netlify 배포와는 관계 없습니다.

### Q: 매번 `npm run build`를 해야 하나요?
**A:** 아니요! Netlify가 자동으로 빌드합니다. 로컬에서 빌드 오류를 확인하고 싶을 때만 사용하세요.

### Q: 푸시 후 얼마나 걸리나요?
**A:** 보통 2-5분 정도 걸립니다. Netlify 대시보드에서 진행 상황을 확인할 수 있습니다.

### Q: 배포가 실패하면?
**A:** Netlify 대시보드의 "Deploys" 탭에서 오류 로그를 확인하세요. 오류를 수정하고 다시 푸시하면 됩니다.

---

## 🎯 요약

**가장 쉬운 방법:**
1. 코드 수정 후 저장
2. `git add .`
3. `git commit -m "설명"`
4. `git push origin main`
5. 끝! Netlify가 자동으로 배포합니다.

**`npm run dev`는 로컬 테스트용일 뿐, 배포와는 무관합니다!**

