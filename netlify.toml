[build]
  command = "npm run build"
  publish = ".next"

# CSP 헤더는 middleware.ts에서 통합 관리하므로 여기서는 제거
# 헤더 충돌을 방지하기 위해 middleware.ts 하나로 통합

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

